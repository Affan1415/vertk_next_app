/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['pbs.twimg.com',"images.unsplash.com","example.com","media.licdn.com"], // Add your allowed domains here
    },
};

export default nextConfig;
